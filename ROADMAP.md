# Roadmap

Direction, not promises. V1 scope is frozen by `IMPLEMENTATION_PLAN.md`; everything here is explicitly out of V1. Finalized narrative is written in Phase 8.

- **Labels-at-source deepening (D3)** — richer classification pushed into the data sources themselves.
- **Abstraction mode (D2)** — planner sees an abstracted schema rather than real object names.
- **Attestable backends (D4)** — verifiable execution environments for the boundary.
- **ERP adapter** — first non-Postgres adapter against a real ERP surface.

## Parking lot

Items deferred out of scope during development land here with a one-line note.

_Empty._
