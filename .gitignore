# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
.coverage
htmlcov/
dist/
build/

# Environment
.env
docker-compose.override.yml

# Editors / OS
.vscode/
.idea/
*.swp
.DS_Store
Thumbs.db

# Build artifacts — kept locally, not published. These served the construction
# of the project, not a reader of it. STATE.md is published, but only as the
# INTERFACE_CHANGE_REQUEST register; the session journal lives in STATE.local.md.
IMPLEMENTATION_PLAN.md
LAUNCH_DRAFTS.md
STATE.local.md

# Audits and study notes written for the author, not for a reader of the repo.
# The whole directory, so a new audit is private by default rather than by
# someone remembering to add its filename here.
docs/
